#include<stdio.h>
int main()
{
  int a=5,b=10,c;
  swapping using extra variable 
  c=a;
  a=b;
  b=c;
 printf("after swapping using extra variable:\n");
 printf("a=%d\n",a);
 printf("b=%d\n",b);
 return 0;
}
